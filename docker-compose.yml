services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - MODEL_PATH=diabetes_model.pkl
    volumes:
      - .:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
